# rysb-dev.github.io
My personal website

## Local development

Start the Zola development server without entering the Nix development shell:

```bash
./serve.sh
```

The server is available at `http://127.0.0.1:1111`. Pass additional Zola options
after the script name if needed:

```bash
./serve.sh --port 8000
```
